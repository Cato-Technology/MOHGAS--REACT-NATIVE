import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  checkbox: {
    width: 18,
    height: 18,
    marginRight: 5,
    borderWidth: 1.5,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
