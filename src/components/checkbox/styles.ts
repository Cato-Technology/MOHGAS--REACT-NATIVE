import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  checkbox: {
    width: 22,
    height: 22,
    marginRight: 5,
    borderRadius: 5,
    borderWidth: 1.5,

    alignItems: 'center',
    justifyContent: 'center',
  },
});
